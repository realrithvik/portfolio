# New chat starter

Copy into a new Cursor chat (or `@` this file):

---

Continue my Astro portfolio at `E:\Portfolio website`. I’m Rithvik (designer). Brand: navy `#15173D`, purple `#982598`, pink `#E491C9`, cream `#F1E9E9`; fonts Erode/Recia.

**Open focus:** Clean up the hero — cream thought-dot must sit tight under the **R** in “Rithvik”; the hand-drawn “how I think” arrow should point at that dot with the label sitting right of the arrow (no floating gap). Prefer CSS; strip any leftover debug `agent log` / ingest instrumentation in `src/pages/index.astro`.

Run with `npm run dev`. Project rules live in `.cursor/rules/portfolio.mdc`.

---

## After you paste

1. Start a **new chat**
2. Paste the block above (or `@HANDOFF.md`)
3. Say what you want next
